/// <reference types="node" />
import { type IncomingMessage } from 'http';
export declare type ModifiedIncomingMessage = IncomingMessage & {
    body(): Promise<Buffer>;
    json(): Promise<Record<string, any>>;
};
export declare function modifyIncomingMessage(message: IncomingMessage): ModifiedIncomingMessage;
